pipeline {
    agent any
    environment {
        secret = credentials('prashant-aws-cred')
    }
    stages {
        stage('build') {
            steps {
               echo "prashant shinde ok"
            }
    
    }
 }
}
